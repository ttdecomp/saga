#include "decomp.h"
struct nufpar_s;

static __used__ void Grab_invert_x(nufpar_s *) { }
static __used__ void Grab_move_xy(nufpar_s *) { }
static __used__ void Grab_radius(nufpar_s *) { }
static __used__ void Grab_rotate(nufpar_s *) { }
static __used__ void Grab_scale(nufpar_s *) { }
static __used__ void Grab_shadow(nufpar_s *) { }
static __used__ void Grab_speed(nufpar_s *) { }
static __used__ void Grab_type(nufpar_s *) { }
