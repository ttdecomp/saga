#!/usr/bin/env bash
#MISE description="Build and run the native host"
#MISE depends=["bazel:host"]
#MISE raw_args=true
set -euo pipefail

if [[ -x bazel-bin/src/saga_host.exe ]]; then
    exec bazel-bin/src/saga_host.exe "$@"
fi
exec bazel-bin/src/saga_host "$@"
