#!/usr/bin/env bash
#MISE description="Build and run the native host"
#MISE depends=["host:build"]
#MISE raw_args=true
set -euo pipefail

if [[ -x build-host/saga.exe ]]; then
    exec build-host/saga.exe "$@"
fi
exec build-host/saga "$@"
