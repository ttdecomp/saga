#!/usr/bin/env bash
#MISE description="Regenerate and validate matching results"
#MISE depends=["match:report"]
set -euo pipefail

if [[ -f res/libTTapp.so ]]; then
    python3 scripts/check_symbols.py
else
    echo "WARNING: res/libTTapp.so not present; skipping symbol check"
fi
